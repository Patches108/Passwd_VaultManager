<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--  HORIZONTAL SCROLLBAR STYLE  -->
    <!--  I don't know why maximum/viewport size 38 works...but it just does. Higher values cut the thumb short...I don't know. Just don't change it  -->
    <Style x:Key="HorizontalScrollBar" TargetType="ScrollBar">
        <Setter Property="Background" Value="SlateGray" />
        <Setter Property="Foreground" Value="YellowGreen" />
        <Setter Property="Maximum" Value="100" />
        <Setter Property="Minimum" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Border
                        x:Name="brdBack"
                        Height="7"
                        Background="{TemplateBinding Background}"
                        CornerRadius="6">
                        <Track
                            x:Name="PART_Track"
                            Height="7"
                            VerticalAlignment="Bottom"
                            Maximum="{TemplateBinding Maximum}"
                            Minimum="{TemplateBinding Minimum}"
                            Orientation="Horizontal"
                            ViewportSize="38"
                            Value="{TemplateBinding Value}">
                            <Track.Thumb>
                                <Thumb
                                    Width="100"
                                    Height="7"
                                    HorizontalAlignment="Left"
                                    Background="{TemplateBinding Foreground}" />
                            </Track.Thumb>
                        </Track>
                    </Border>

                    <ControlTemplate.Triggers>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="brdBack" Property="Background" Value="Gray" />
                            <Setter TargetName="brdBack" Property="BorderBrush" Value="Gray" />
                        </Trigger>

                    </ControlTemplate.Triggers>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>